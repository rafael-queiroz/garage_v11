package br.com.rqueiroz.material.config;

/*
@Configuration
@EnableSwagger2
public class SwaggerConfig {

    @Bean
    public Docket api() {
        return new Docket(DocumentationType.SWAGGER_2)
                .select()
                .apis(RequestHandlerSelectors.basePackage("br.com.rqueiroz"))
                .paths(PathSelectors.any())
                .build()
                .apiInfo(apiInfo());
    }

    private ApiInfo apiInfo() {
        return new ApiInfo(
                "Garage Material!",
                "Microservices Material.",
                "v1",
                "Terms Of Service Url",
                new Contact("Rafael Queiroz", "wwww.rqueiroz.com.br", "rafael.qp@gmail.com"),
                "License of API", "License of URL", Collections.emptyList());
    }

}*/
